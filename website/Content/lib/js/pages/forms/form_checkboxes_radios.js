//----------------------------------
//   File          : js/pages/forms/form_checkboxes_radios.js
//   Type          : JS file
//   Version       : 1.0.0
//   Last Updated  : April 4, 2017
//----------------------------------


// Primary
$(".control-primary").uniform({
	radioClass: 'choice',
	wrapperClass: 'border-primary text-primary'
});

// Danger
$(".control-danger").uniform({
	radioClass: 'choice',
	wrapperClass: 'border-danger text-danger'
});

// Success
$(".control-success").uniform({
	radioClass: 'choice',
	wrapperClass: 'border-success text-success'
});

// Warning
$(".control-warning").uniform({
	radioClass: 'choice',
	wrapperClass: 'border-warning text-warning'
});

// Info
$(".control-info").uniform({
	radioClass: 'choice',
	wrapperClass: 'border-info text-info'
});

// Custom color
$(".control-custom").uniform({
	radioClass: 'choice',
	wrapperClass: 'border-indigo-200 text-indigo-200'
});
